spring:
  datasource:
    url: jdbc:h2:mem:emotionalHelp
    username: team3
    password: qwerty
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true